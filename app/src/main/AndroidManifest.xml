<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.assistapp">

    <!-- 권한 선언 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <!-- 선택적 카메라 기능 -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Assistapp"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- 메인 액티비티 -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 기능 액티비티 -->
        <activity android:name=".Feature1Activity" />
        <activity android:name=".Feature2Activity" />
        <activity android:name=".Feature3Activity" />
        <activity android:name=".FeatureManagerActivity" />

        <!-- 카메라 액티비티 -->
        <activity
            android:name=".features.camera.CameraActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- 위치 공유 관련 -->
        <activity
            android:name=".features.LocationSharing.LocationSharingWithCodeActivity"
            android:exported="true" />

        <activity
            android:name=".features.Schedule.ScheduleSendScreenActivity"
            android:exported="true" />

        <!-- 웹뷰 -->
        <activity
            android:name=".ui.WebViewScreenActivity"
            android:exported="true" />

        <!-- 일정 공유 관련 (Compose Screen 호출) -->
        <activity
            android:name=".features.ScheduleSharing.ScheduleSendScreenActivity"
            android:exported="true" />

        <!-- 카카오맵 앱 키 -->
        <meta-data
            android:name="com.kakao.vectormap.APP_KEY"
            android:value="18ce207a640e4a1f61001296ba153fde" />

        <!-- 백그라운드 위치 추적 서비스 -->
        <service
            android:name=".services.LocationTrackingService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />

        <activity
            android:name=".features.ScheduleSharing.ScheduleListActivity"
            android:exported="true" />

    </application>
</manifest>
